// This is a template for the config file.
// Replace the values with your own API key and base URL.
// Then, rename the file to config.ts.
const config = {
    weatherApi: {
        key: 'YOUR_API_KEY_HERE', // Replace with your API key
        baseUrl: 'https://api.weatherapi.com/v1',
    },
};
export default config;
//# sourceMappingURL=config.template.js.map